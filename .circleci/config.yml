version: 2.1

orbs:
  python: circleci/python@0.2.1

jobs:
  test:
    executor: python/default
    steps:
      - checkout
      - run:
          command: python3 -u -m unittest discover
          name: Test

workflows:
  main:
    jobs:
      - test
